name: test

on: 
  workflow_dispatch:

jobs:
  build:
    name: "✏️ test case"
    runs-on: ubuntu-18.04
    steps:
      - name: "📥 Check-out"
        uses: actions/checkout@v2
        run: echo "checkout"
      
